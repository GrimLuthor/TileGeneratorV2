namespace TileGeneratorV2.Core.Structure;

public enum WeatheringPreset
{
    Clean,     // minimal weathering, no erosion
    Weathered, // seed-derived natural range, moderate chips
    Broken     // heavily worn, aggressive edge erosion
}
